/*
let y = new Float32Array(N);
for (let i = 0; i < 2*N-3; i++) {
    for (let idx = 0; idx < N; idx++) {
        y[idx] = insertionSortNetwork(x, i, idx);
    }
    let s = "";
    for (let idx = 0; idx < N; idx++) {
        x[idx] = y[idx];
        s = s + x[idx] + ", ";
    }
    console.log(s);
    //x = regIter(x, i);
}*/
